<% content_for :head do %>
  <%= t '.title' %>
<% end %>

<% if flash[:notice] %>
    <div class="alert alert-success alert-dismissible">
      <%= flash[:notice] %>
    </div>
<% end %>

<%= render "search"%>


<div class="box box-primary">
<div class="box box-default">
  <!-- /.box-header -->
  <div class="box-body no-padding">
    <table class="table table-hover">
      <tr>
        <th style="width: 10px"></th>
        <th><%= t '.description' %></th>
      </tr>

      <% @content_summaries.each do |content_summary| %>
      <tr onclick="location.href='<%= edit_content_summary_path(content_summary) %>'" style="cursor:pointer">
        <td><%= content_summary.id %></td>
        <td><%= content_summary.description %></td>
      </tr>
    <% end %>
    </table>
  </div>
  <!-- /.box-body -->
</div>
<!-- /.box -->
</div>

<% if @content_summaries.size > 0 %>
<%= paginate @content_summaries %>
<% end %>
